# Крок 07 — Медіа та брендинг: іконка, splash, підключення звуку

**Статус: 🟡 виконано, окрім рішення по вазі APK** · Залежності: крок 05 (палітра для іконки/splash)
**Вхідні дані:** [general_alpha_2_0.md](general_alpha_2_0.md) (крок 07), **[sounds_and_music_map.md](../sounds_and_music_map.md)** — мапа готових аудіо-асетів, рев'ю alpha 1.0 («Аудіо/візуал — найслабша область»)

## Мета

Гра стає впізнаваною і «звучною»: власна іконка застосунку, темний splash, **усі готові аудіо-асети підключені за мапою** — плейлист з 10 внутрішньоігрових треків, клік-звуки, луп клавіатури під час атаки. Крок підключає наявне, а не шукає нове.

## Контекст (фактичний стан, 2026-07-05)

- **Аудіо в репозиторії:** `main_theme.mp3` (грає) + **10 треків** `background_tracks/NN_ingame_back.mp3` + **5 SFX** у `sound_effects/`: `mouse-click`, `message_sound`, `keyboard`, `button`, `light-switch` (останній у мапі відсутній).
- **Мапа призначень** (sounds_and_music_map.md): main_theme — меню/auth; 10 треків — внутрішньоігровий фон; mouse-click — будь-який клік; message_sound — level-up; keyboard — **луп під час атаки**; button — кнопка запуску атаки.
- **Розрив із кодом:** константи `AppAudio` ([constants.dart:23-31](../../../lib/utils/constants.dart)) вказують на 7 файлів `sfx_*.mp3`, яких **не існує**; нові під-теки `background_tracks/` і `sound_effects/` **не декларовані в pubspec** (тека `assets/audio/` не рекурсивна) — жоден новий файл зараз не потрапляє в білд.
- `AudioService` вміє один BGM-трек і разові SFX; лупів SFX і плейлистів немає.
- `flutter_launcher_icons ^0.14.3` і `flutter_native_splash ^2.4.5` вже в dev_dependencies — не сконфігуровані.
- APK 66.3 МБ **до** додавання 11 треків — вага зросте, заміряти.

## Задачі

### 7.1 Іконка застосунку

- [x] Майстер-арт 1024×1024 згенеровано зі спрайта `hacker_green.png` (819×819) на фоні #0A0A0C — **рішення власника від 2026-07-18**. Файли в `assets/branding/`: `app_icon.png` (спрайт 72%), `app_icon_foreground.png` (55%, під safe zone adaptive-маски), `splash_logo.png`.
- [x] Конфіг `flutter_launcher_icons` у pubspec: Android adaptive (foreground + фон #0A0A0C, `min_sdk_android: 21`) + Windows .ico 256px. Згенеровано.
- [ ] Візуальна перевірка на пристрої (світлий/темний лончер, обрізання маскою) — потребує фізичного Android, крок 09.

### 7.2 Splash-екран

- [x] Конфіг `flutter_native_splash`: фон #0A0A0C, по центру `splash_logo.png`, секція `android_12:` окремо. Згенеровано.
- [ ] Перевірка холодного старту на пристрої — крок 09.
- ⚠️ Ключ `windows:` ця версія `flutter_native_splash` **відхиляє** як невалідний параметр (падає з помилкою). Splash налаштований для Android; Windows-раннер білого спалаху не дає.

### 7.3 Підключення SFX за мапою

- [x] pubspec: під-теки додано ще на кроці 06 — усі 15 файлів у білді.
- [x] Переписано константи `AppAudio` під реальні файли; мапа підключень:
  - `mouse-click.mp3` → універсальний клік (навігація, вибір, back) — один хелпер/обгортка кнопки, не 50 ручних викликів;
  - `button.mp3` → LAUNCH ATTACK (7.1) і старт міні-гри;
  - `message_sound.mp3` → level-up (замінює неіснуючий `sfx_level_up`);
  - `keyboard.mp3` → **луп на час атаки** (Screen 7.2): нова здатність `AudioService.startLoopSfx/stopLoopSfx` — старт при вході в міні-гру/auto-resolve, стоп при завершенні/abort; пауза разом із `pauseEngine`.
- [x] Хуки без власних клипів змаплено на наявні через семантичні аліаси в `AppAudio` (в одному місці, не по call-site'ах): catch→click, hit/lose→button, win/upgrade/level-up→message, purchase→click. Прогалини занесено в мапу і в беклог анкети плейтесту.
- [x] `light-switch.mp3` призначено на перемикач Mute All і прев'ю гучності в Settings — **рішення власника від 2026-07-18**.
- [x] Гучність: `applyVolume()` тепер покриває і луп; Settings застосовує зміни наживо, а не лише при виході; луп стартує навіть при нульовій гучності, щоб слайдер посеред атаки спрацював одразу.

### 7.4 BGM: плейлист хаба

- [x] `AudioService`: плейлист із **9** унікальних треків (09 видалено як дублікат 01 — рішення власника), shuffle без повтору підряд включно зі стиком між колами, авто-перехід по `onPlayerComplete`; API `playHubPlaylist()` / `playMenuTheme()`.
- [x] Перемикання меню ↔ хаб — через `AudioRouteObserver` (`NavigatorObserver`), а не вручну по екранах. Settings свідомо не перемикає музику: екран доступний з обох боків.
- [x] Гучність — та сама формула; пауза при згортанні успадкована від `Bgm` (`WidgetsBindingObserver`).
- [ ] Кросфейд між треками — **не зроблено**: `Bgm` API не дає накладання, потрібен другий плеєр і ручний фейд. Перехід зараз різкий. Винесено в беклог, не блокує альфу.

### 7.5 Вага збірки — замір і рішення

- [x] Release APK зібрано, розмір зафіксовано. **Результат: 104.04 МБ** (було 66.3 МБ до підключення медіа).

| Категорія            | Стиснуто | Файлів |
| -------------------- | -------- | ------ |
| Нативні бібліотеки   | 57.02 МБ | 15     |
| **Аудіо**            | **41.26 МБ** | 15 |
| Відео (media_kit bg) | 2.98 МБ  | 1      |
| Зображення           | 1.42 МБ  | 15     |
| Інше                 | 1.10 МБ  | 390    |
| Шрифти               | 0.08 МБ  | 3      |

- [ ] 🔴 **Рішення по бітрейту — за власником.** Треки закодовані в **256 kbps**, main_theme — **320 kbps**. Для фонової ігрової музики це надлишок у 2–2.5 рази. Перекодування в 128 kbps дало б приблизно −20 МБ (APK ~104 → ~84 МБ). **Не виконано**: ffmpeg на машині не встановлено, а перекодування — операція з втратами, що незворотно псує вихідні файли. Потрібне явне рішення власника + бекап оригіналів.
- Рішення по `matrix_bg.mp4` (2.98 МБ — не проблема) — після фідбеку тестерів (крок 09).

### 7.6 Документація

- [x] `docs/design/media_presets.md` створено; `sounds_and_music_map.md` оновлено фактичними підключеннями (включно з light-switch і таблицею спільних клипів).
- [x] `CREDITS.md` заповнено (2026-07-18): усе аудіо — Pixabay під Pixabay Content License (атрибуції не вимагає); уся графіка — оригінальний арт дизайнера проєкту, стороннім контентом не є. Відкритих ліцензійних питань немає.

## Критерії приймання

- [ ] Android-лончер і Windows-таскбар — власна іконка; холодний старт — темний splash.
- [ ] У хабі грає плейлист (мінімум 3 різні треки за сесію перевірки), у меню — main_theme; переходи плавні.
- [ ] Клік чутно на навігації, level-up озвучений, під час атаки — луп клавіатури, що коректно стартує/стопається/паузиться; Mute All дає повну тишу.
- [ ] Кожен файл з `assets/audio/` або підключений, або свідомо видалений — «мертвих» асетів у білді немає.
- [ ] `CREDITS.md` покриває всі сторонні активи; `flutter analyze` — 0 issues; release-білди обох платформ зібрані, розмір зафіксовано.

## Поза межами кроку

- Пошук додаткових SFX для непокритих хуків — за результатами плейтесту (крок 09), тут лише мапінг наявного.
- Заміна відео-фону — окреме рішення після плейтесту.
- Release-підпис APK — блокер лише для сторів, не для роздачі тестерам.

## Ризики

| Ризик | Мітигація |
| --- | --- |
| Луп SFX через audioplayers конфліктує з BGM media_kit/двома плеєрами | Прототип лупа першим підзавданням 7.3; якщо конфліктує — окремий інстанс плеєра для лупів |
| 10 треків помітно роздують APK | Замір у 7.5 до релізу; бітрейт/скорочення плейлиста як швидкий відкат |
| Глобальний клік-звук дратує | Один хелпер = одна точка вимкнення; питання в анкету плейтесту |
